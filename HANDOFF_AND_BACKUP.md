# سند جامع معماری، هندآف و پشتیبان پروژه (Project Handoff & Architecture Backup)
## سوئیت مدیریت حساب‌ها، سهمیه و مهاجرت پروژه‌های گوگل آنتی‌گرویتی (Antigravity Account Switcher & Project Migration Suite)

**تاریخ ایجاد سند:** ۱۰ سپتامبر ۲۰۲۶ (۲۰ شهریور ۱۴۰۵)  
**مسیر ریشه پروژه:** `C:\Users\gerap\Desktop\agent-helper\gravity suitch accont`  
**آدرس مخزن رسمی گیت‌هاب:** `https://github.com/Madgod-xyz/antigravity-account-switcher.git`  
**توسعه‌دهنده و مالک معنوی:** **Madgod-xyz**  
**شاخه فعال:** `master`  

---

## ۱. معرفی و هدف پروژه (Project Mission)
این پروژه یک ابزار جامع، بومی و سبک برای کاربران و توسعه‌دهندگان **Google Antigravity** است. هدف اصلی این سوئیت:
1. **حل چالش سوئیچ بین چند حساب گوگل:** بدون نیاز به خروج دستی و لاگین مجدد، در کمتر از ۳ ثانیه اعتبار فعال در سیستم‌عامل تعویض شده و محیط ادیتور با حساب جدید بالا می‌آید.
2. **مرکز مهاجرت و کلون پروژه‌ها و مکالمات ایجنت:** وقتی سهمیه یک اکانت تمام می‌شود یا پروژه‌ای در حساب اول توسعه یافته، کاربر می‌تواند با یک کلیک گفتگوها، مغز ایجنت (Brain Artifacts) و دیتابیس‌های پروژه را به اکانت دوم کپی (Clone) یا منتقل (Move) کند.
3. **رابط کاربری لوکس شیشه‌ای مایع (Apple iOS Liquid Glass):** تجربه‌ای با بلور عمیق، جلوه‌های نوری کریستالی و فیزیک انیمیشن اسپرینگ ۶۰ فریم فوق‌العاده نرم.
4. **پشتیبانی بین‌المللی ۴ زبانه:** انگلیسی (EN)، فارسی راست‌چین با فونت وزیرمتن (FA)، چینی ساده‌شده (ZH) و اسپانیایی (ES).
5. **یکپارچگی و عدم تداخل با پلاگین سهمیه (`gravity plugin`):** هر دو ابزار در کنار هم بدون کمترین اصطکاک کار می‌کنند.

---

## ۲. ساختار دایرکتوری و فایل‌ها (Directory Structure)

```text
gravity suitch accont/
├── app/                                # هسته رابط کاربری شیشه‌ای مایع iOS
│   ├── index.html                      # ساختار صفحه با کارت‌های شیشه‌ای و مدال مهاجرت
│   ├── styles.css                      # استایل Liquid Glass، بلور 40px، حاشیه‌های نوری و فیزیک اسپرینگ
│   ├── app.js                          # مدیریت وضعیت، رندر کارت‌ها، تایمر معکوس و ارتباط با سرور
│   └── i18n.js                         # دیکشنری کامل ترجمه به ۴ زبان زنده دنیا (EN, FA, ZH, ES)
├── migration_engine.py                 # موتور اسکن، کلون، انتقال و تجمیع دیتابیس‌ها و مغز ایجنت
├── quota_engine.py                     # موتور استخراج سهمیه زنده، تفکیک مدل‌ها و رتبه اشتراک اکانت
├── server.py                           # وب‌سرور چندنخی لوکال (پورت 39285) جهت میزبانی UI و درخواست‌های IPC
├── switcher_windows.ps1                # اسکریپت ویندوز (مدیریت advapi32.dll، وب‌ویو و CLI)
├── switcher_core.py                    # هسته مک و لینوکس (مدیریت macOS Keychain و CLI)
├── install.ps1                         # نصب‌کننده خودکار ویندوز (شورتکات دسکتاپ، استارت‌منو و PATH)
├── install.sh                          # نصب‌کننده مک با کامپایل باینری یونیورسال
├── AntigravitySwitcher.vbs             # لانچر بدون کنسول و بی‌صدای ویندوز
├── AntigravitySwitcher.bat             # لانچر سریع دسکتاپ
├── README.md                           # راهنمای رسمی بین‌المللی ریپازیتوری در گیت‌هاب
├── HANDOFF_AND_BACKUP.md               # همین سند هندآف و مستندات فنی کامل
└── assets/                             # آیکون‌ها، بنرها و تصاویر رسمی ریپازیتوری
```

---

## ۳. معماری فنی و جریان داده (Technical Deep-Dive)

### ۱. مکانیزم احراز هویت سیستم‌عامل (Credential Vault Interop)
* **ویندوز (Windows Credential Manager):**
  - از طریق توابع C# P/Invoke متصل به کتابخانه `advapi32.dll` توکن‌ها در اهداف `gemini:antigravity` و `gemini` خوانده و نوشته می‌شوند.
  - ساختار توکن به صورت رشته خام JSON یا Base64 گو-کی‌رینگ (`go-keyring-base64:`) شناسایی و پارس می‌شود.
* **مک (macOS Keychain):**
  - ارتباط مستقیم با دستور نیتیو `security find-generic-password` و `security add-generic-password` برای سرویس `gemini` و اکانت `antigravity`.

### ۲. پایش بلادرنگ سهمیه و اشتراک (`quota_engine.py`)
* **بازیابی و رفرش توکن:** در صورت منقضی شدن `access_token`، به صورت خودکار با استفاده از `refresh_token` و کلید‌های کلاینت گوگل توکن تازه دریافت می‌شود.
* **استعلام رتبه اشتراک:** از طریق متد `loadCodeAssist` رتبه کاربر دیتکت می‌شود (`Free`, `Google AI Pro`, `Ultra`, `Enterprise`).
* **تفکیک ۴ مدل اصلی هوش مصنوعی:**
  - `Gemini 3.8 Flash High`
  - `Gemini 3.1 Pro`
  - `Claude Sonnet 4.6`
  - `GPT-OSS 120B`
* **تایمر دقیق بازنشانی:** استخراج زمان ریست ۵ ساعته و تبدیل به ثانیه‌شمار معکوس زنده.

### ۳. موتور هوشمند انتقال و مهاجرت (`migration_engine.py`)
* **موقعیت داده‌های محلی آنتی‌گرویتی:**
  - دیتابیس‌های مکالمات: `~/.gemini/antigravity/conversations/<id>.db`
  - مغز ایجنت (ترنسکریپت‌ها، لاگ‌ها و آرتیفکت‌ها): `~/.gemini/antigravity/brain/<id>/`
* **مکانیزم پشتیبان‌گیری ایمن (Safety First):** قبل از هرگونه تغییر یا انتقال، یک بکاپ از مکالمات هدف در مسیر `~/.gemini/antigravity-backup/` ذخیره می‌شود.
* **حالت‌های انتقال:**
  - **Safe Copy (کپی ایمن):** یک UUID جدید تولید کرده، رکورد `cascade_id` را در جدول `trajectory_meta` دیتابیس SQLite بازنویسی می‌کند و مغز ایجنت را کپی می‌کند.
  - **Cut & Move:** مکالمه و آرتیفکت‌ها را به هویت مقصد منتقل کرده و فایل‌های مبدا را پاکسازی می‌کند.
  - **Merge:** گام‌های چند گفتگوی مختلف را از جدول `steps` خوانده و در یک تایم‌لاین یکپارچه تجمیع می‌کند.
  - **Continuous Dual-Sync:** نگاشت زوج‌های همگام در فایل `sync_manifest.json`.

### ۴. رابط کاربری شیشه‌ای مایع (`app/`) و وب‌سرور (`server.py`)
* **طراحی Liquid Glass:** استفاده از ترکیب `-webkit-backdrop-filter: blur(40px) saturate(180%)` با لایه‌های کریستالی نورانی (`specular borders`).
* **فیزیک اسپرینگ:** انیمیشن‌های نرم دکمه‌ها و باز شدن مودال مشابه سیستم‌عامل iOS با ترنزیشن‌های `cubic-bezier(0.16, 1, 0.3, 1)`.
* **وب‌سرور سبک لوکال:** وب‌سرور پایتون روی پورت `39285` مستقر شده و در صورت اشغال بودن پورت به طور خودکار پورت آزاد بعدی را انتخاب می‌کند.
* **لانچ در حالت مستقل (App Mode):** مرورگر Edge یا Chrome در وضعیت `--app=http://127.0.0.1:39285/index.html` پنجره‌ای بدون آدرس‌بار و حاشیه‌های مزاحم باز می‌کند.

---

## ۴. راهنمای دستورات و تست (Testing & Commands)

| دستور | کاربرد |
| :--- | :--- |
| `agy-switch` | باز کردن رابط کاربری گرافیکی شیشه‌ای iOS |
| `agy-switch --usage` | نمایش کارت زیبا و اسکی سهمیه در ترمینال |
| `agy-switch --list` | لیست تمام حساب‌های ذخیره‌شده و اکانت فعال |
| `agy-switch --switch <email>` | سوئیچ آنی به جیمیل مورد نظر و ری‌استارت ادیتور |
| `agy-switch --save` | ذخیره سشن و توکن اکانت فعال فعلی در دیسک |
| `agy-switch --logout` | خروج از اکانت فعال جهت لاگین به جیمیل جدید |
| `agy-switch --migrate` | دریافت خروجی JSON از مکالمات محلی جهت مهاجرت |
| `agy-switch --about` | نمایش مشخصات سازنده و لینک گیت‌هاب Madgod-xyz |

---

## ۵. اقدامات لازم برای انتشار روی گیت‌هاب (Publishing Steps)

1. مخزن محلی با مشخصات نویسنده زیر پیکربندی شده است:
   - نام نویسنده: `Madgod-xyz`
   - ایمیل: `madgod.cum@gmail.com`
   - ریموت گیت‌هاب: `https://github.com/Madgod-xyz/antigravity-account-switcher.git`
2. برای انتشار کافیست در ترمینال دستورات زیر اجرا شود:
   ```bash
   git add .
   git commit -m "feat: complete iOS Liquid Glass suite, project migration hub, and multi-language support"
   git push -u origin master
   ```
